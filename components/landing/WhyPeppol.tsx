export function WhyPeppol() {
  return (
    <section className="border-t border-border/50 py-16 sm:py-20">
      <div className="mx-auto max-w-5xl px-4 sm:px-6">
        <div className="max-w-2xl space-y-4">
          <h2 className="text-xl font-semibold tracking-tight">
            Why this matters now
          </h2>
          <p className="text-muted-foreground">
            From 1 July 2026, all Australian government agencies must accept and
            send Peppol e-invoices. If your invoice isn&apos;t Peppol-compliant,
            it gets rejected — not delayed, rejected.
          </p>
          <p className="text-muted-foreground">
            No more chasing payments by email. No more PDFs lost in inboxes. One
            standard, machine-readable format that goes directly into their
            accounts payable system.
          </p>
        </div>
      </div>
    </section>
  );
}
